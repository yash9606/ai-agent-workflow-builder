table:
  name: audit_logs
  schema: public
object_relationships:
  - name: organization
    using:
      foreign_key_constraint_on: org_id
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - org_id
        - user_id
        - action
        - resource_type
        - resource_id
        - details
        - created_at
      filter:
        organization:
          members:
            user_id:
              _eq: X-Hasura-User-Id
