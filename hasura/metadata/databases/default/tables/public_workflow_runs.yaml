table:
  name: workflow_runs
  schema: public
object_relationships:
  - name: workflow
    using:
      foreign_key_constraint_on: workflow_id
array_relationships:
  - name: step_runs
    using:
      foreign_key_constraint_on:
        column: workflow_run_id
        table:
          name: step_runs
          schema: public
  - name: workflow_db_writes
    using:
      foreign_key_constraint_on:
        column: workflow_run_id
        table:
          name: workflow_db_writes
          schema: public
  - name: notifications
    using:
      foreign_key_constraint_on:
        column: workflow_run_id
        table:
          name: notifications
          schema: public
  - name: usage_events
    using:
      foreign_key_constraint_on:
        column: workflow_run_id
        table:
          name: usage_events
          schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - workflow_id
        - triggered_by
        - trigger_type
        - status
        - started_at
        - completed_at
        - error
        - created_at
      filter:
        workflow:
          organization:
            members:
              user_id:
                _eq: X-Hasura-User-Id
